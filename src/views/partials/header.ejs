<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Roam</title>
    <link rel="stylesheet" type="text/css" href="/style.css">
  </head>
  <body>
    <div>
      <header>
        <h3><a href="/">Roam</a></h3>
        <h3><%= city.name %></h3>
        <span>
          <% if (!isLoggedIn) {%>
            <a class="sign-up" href="/signup">Sign Up</a>
            <a class="log-in" href="/login">Log In</a>
          <%} else { %>
            <a class="private-profile" href="/profile/private">Profile</a>
            <a class="log-out" href="/logout">Log Out</a>
          <%}%>
        </span>
      </header>
    </div>
